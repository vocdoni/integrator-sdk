import { http, HttpResponse } from 'msw'

export const handlers = [
  // HTTP handlers are added here as packages are implemented.
  // Example:
  // http.get('/api/v1/process/:id/metadata', ({ params }) =>
  //   HttpResponse.json({ id: params.id, title: 'Test election' })
  // ),
]
