// Balance is not available in the new SaaS API (no on-chain token balance).
// This stub is exported for API compatibility. It renders nothing.
import { ComponentPropsWithoutRef } from 'react'

export const Balance = (_props: ComponentPropsWithoutRef<'span'>) => null
